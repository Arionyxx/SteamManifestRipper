{
  "name": "electron-app",
  "version": "1.0.0",
  "description": "Electron 30 app with Tailwind CSS and DaisyUI",
  "main": "main.js",
  "scripts": {
    "start": "npm run build:tailwind && electron .",
    "build:tailwind": "tailwindcss -i ./src/styles.css -o ./src/output.css",
    "watch:tailwind": "tailwindcss -i ./src/styles.css -o ./src/output.css --watch",
    "test": "node src/tests/vdf-parser.test.js && node src/tests/config-reader.test.js && node src/tests/app-loader.test.js && node src/tests/registry-reader.test.js && node src/tests/integration.test.js && node src/tests/renderer.test.js"
  },
  "keywords": [
    "electron",
    "tailwindcss",
    "daisyui"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "electron": "^39.0.0",
    "tailwindcss": "^3.4.0"
  },
  "dependencies": {
    "daisyui": "^4.0.0"
  }
}
