{
  "name": "steam-depot-manager",
  "version": "1.0.0",
  "description": "Steam Depot Manifest Manager with Tailwind CSS and DaisyUI",
  "main": "main.js",
  "scripts": {
    "start": "npm run build:tailwind && electron .",
    "dev": "npm run watch:tailwind & electron .",
    "build:tailwind": "tailwindcss -i ./src/styles.css -o ./src/output.css --minify",
    "watch:tailwind": "tailwindcss -i ./src/styles.css -o ./src/output.css --watch",
    "prebuild": "npm run build:tailwind",
    "build": "electron-packager . SteamDepotManager --platform=win32 --arch=x64 --out=build-output --overwrite --icon=icon.ico",
    "test": "node src/tests/vdf-parser.test.js && node src/tests/config-reader.test.js && node src/tests/app-loader.test.js && node src/tests/registry-reader.test.js && node src/tests/integration.test.js && node src/tests/renderer.test.js && node src/tests/settings-store.test.js"
  },
  "keywords": [
    "electron",
    "tailwindcss",
    "daisyui",
    "steam",
    "depot"
  ],
  "author": "AriFemboy",
  "license": "MIT",
  "devDependencies": {
    "electron": "^39.0.0",
    "electron-packager": "^17.1.2",
    "tailwindcss": "^3.4.0"
  },
  "dependencies": {
    "daisyui": "^4.0.0"
  }
}
